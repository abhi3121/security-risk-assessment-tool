# /spec-implement

Run the `implement` capability of the `skill-spec-driven-dev` skill.

1. Read `<SKILL_PATH>/SKILL.md` and `<SKILL_PATH>/rules-core.md`.
2. Read the feature workspace `status.json` to confirm preconditions.
3. Follow `<SKILL_PATH>/capabilities/implement.md` exactly.
