# /spec-pipeline

Run the `pipeline` capability of the `skill-spec-driven-dev` skill.

Read `<SKILL_PATH>/SKILL.md` and `<SKILL_PATH>/rules-core.md` first, then
follow `<SKILL_PATH>/capabilities/pipeline.md` exactly. Check the feature
workspace `status.json` before advancing any stage.
