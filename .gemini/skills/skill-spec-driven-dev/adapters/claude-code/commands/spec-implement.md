# /spec-implement

Run the `implement` capability of the `skill-spec-driven-dev` skill.

Read `<SKILL_PATH>/SKILL.md` and `<SKILL_PATH>/rules-core.md` first, then
follow `<SKILL_PATH>/capabilities/implement.md` exactly. Check the feature
workspace `status.json` before advancing any stage.
