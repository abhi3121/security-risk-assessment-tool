# /spec-init

Run the `init` capability of the `skill-spec-driven-dev` skill.

Read `<SKILL_PATH>/SKILL.md` and `<SKILL_PATH>/rules-core.md` first, then
follow `<SKILL_PATH>/capabilities/init.md` exactly. Prompt the user for the
Jira ticket id and the workspace path (default: `.specs/<FEATURE_KEY>/`).
