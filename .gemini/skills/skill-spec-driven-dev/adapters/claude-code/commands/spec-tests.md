# /spec-tests

Run the `tests` capability of the `skill-spec-driven-dev` skill.

Read `<SKILL_PATH>/SKILL.md` and `<SKILL_PATH>/rules-core.md` first, then
follow `<SKILL_PATH>/capabilities/tests.md` exactly. Check the feature
workspace `status.json` before advancing any stage.
